Implemented src/parse.rs: Prolog AST (Term/Clause/ClauseKind), AtomTable+VarTable interning, recursive-descent parser, list desugaring to '.'/2 cons-nil, \+ wrapping, ?-/directive clauses. Used flat TermIdx arena per-clause (deviation from literal spec to break Rust's size recursion on BoundedArr<Term,8>; more SNOBOL4-faithful anyway). Added Token enum to tokenize.rs (needed now since parse precedes tokenize in reordered plan). Added Debug/PartialEq/Eq derives to BoundedArr. 20 parse tests + all prior 14 pass.