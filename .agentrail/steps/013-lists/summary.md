Lists land: Term::Nil and Term::Struct compile in head (GET_STRUCT + UNIFY_VAR/VAL/CONST stream) and body (new emit_build_list walks the cons spine, writing each head via UNIFY_CONST and threading a cursor X-reg through the tails). refvm gains UnifyMode (Read/Write) + UP, with GET_STRUCT dispatching on Ai's tag (STR → read-mode functor check; REF → write-mode build), and UNIFY_VAR/VAL/CONST executing mode-dependent read-or-write on the heap. parse.rs interns '[]' when a list is encountered so compile.rs can resolve Nil via atoms.find. asm.rs adds UNIFY_CONST (opcode 24, Shape::ConstOnly) and relaxes atom-name validation in .atom directives to accept '.' and '[]' as names. examples/member.pl + compiled_member_prints_each_element_via_backtracking integration test captures 'a\nb\nc\n' end-to-end. 119 lib + 25 integration tests green.