Negation-as-failure \+/1 + unification =/2 as inline builtins. Token::Eq added; parse_cmp_expr accepts '=' alongside '<' and '>'; Token::Not recurses into parse_cmp_expr so '\+ X = Y' parses as '\+(X = Y)'. emit_eq compiles unification as a 4-instruction pattern (PUT LHS A0 + GetVar scratch A0 + PUT RHS A1 + GetVal scratch A1). emit_negation emits TRY alt + G + TRUST + FAIL + alt_label + TRUST — both TRUST instructions reference the same CP pushed by TRY; G-success path pops via first TRUST then FAILs to the outer scope; G-failure path arrives at alt via backtrack and pops via the second TRUST. No reliance on scoped cut, so nested backtracking works correctly. RegMap gained pname/clause_idx/neg_ix fields for unique negation labels. 119 lib + 35 integration tests green.