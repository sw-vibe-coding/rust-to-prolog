Arithmetic builtins is/2, </2, >/2 land. tokenize adds Plus/Minus/Lt/Gt tokens; parse gains a small expression layer (is/cmp/add/atom precedences building Struct terms); compile recognizes the three ops as inline builtins and evaluates flat RHS (Int, Var, +/-) into A0 for is/2, then unifies LHS via GetVar/GetYVar/GetVal; comparisons eval both sides into A0/A1 then emit B_LT/B_GT. refvm dispatches GET_VAL + B_IS_ADD/SUB/LT/GT using 21-bit sign-extended i32 arithmetic. Term::Int now flows through head (GetInt) and body (PutInt) arg positions; repeated head vars (sum(0, Acc, Acc) base case) emit GET_VAL on the second occurrence. examples/sum.pl (tail-recursive sum with accumulator) compiles and refvm-runs: '?- sum(3, 0, X), write(X), nl.' -> '6\n'. 119 lib + 28 integration tests green.