Added write/1, nl/0, fail/0 inline builtins. compile.rs recognizes them by functor+arity and emits B_WRITE / B_NL / FAIL directly; classifier now treats inline builtins as non-chunk-boundary (only non-last user-pred CALLs create chunks). Query body generalized to multi-goal shape with DEALLOCATE+HALT terminator. refvm B_WRITE takes an atoms table and prints names ('red') instead of atom IDs ('atom(1)') when the table is populated; new run_with_atoms entry point threads the parse-time AtomTable through. examples/color.pl compiles and runs end-to-end on refvm; 'red\ngreen\nblue\n' captured, verdict Fail after color's alternatives exhaust. 119 lib + 24 integration tests green.