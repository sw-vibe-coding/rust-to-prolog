Reference LAM VM implementing the ancestor-subset opcodes per vm-spec.md. src/refvm split across mod/heap/choice/dispatch/builtin; heap+trail+choice as Vec (port-audit relaxed per step prompt, documented at top of mod.rs); 17 refvm unit tests + 11 scenario integration tests pass (fact_lookup, fact_fail, put_var, put_var_shared, try/trust, backtrack, two_facts_fail, retry_3cls fail+middle, b_write+b_nl, compiled_parent via the full Rust pipeline). Known caveat surfaced: compile.rs omits ALLOCATE/DEALLOCATE for recursive rules, so the current ancestor.lam fixture will loop on a correct VM — tracked as step 010's concern, not this step's.