Added tests/integration/ancestor_parity.rs — 7 structural invariants pinning the opcode set, env-frame balance, CALL/EXECUTE/TRY/TRUST/Y-slot counts, and terminator of the Rust pipeline's ancestor.lam. Documents the intentional differences from upstream's hand-written LOAD_ANCESTOR_COMPILED (ALLOCATE 2 conservative vs ALLOCATE 1 hand-optimized) without enforcing byte-match. Refreshed docs/demo-plan-status.md with a 'Real-VM status' subsection explaining why step 020-integration-ancestor is blocked upstream. 117 lib + 22 integration tests green.