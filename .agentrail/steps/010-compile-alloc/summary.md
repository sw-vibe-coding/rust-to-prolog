Fixed the semantic bug that step 005 left behind: compile.rs now classifies vars by chunk (chunk = head + first body goal, then each subsequent body goal), marks those appearing in more than one chunk as permanent (Y-reg in env frame), and emits ALLOCATE/DEALLOCATE for rules with any perm vars. Head perms use GET_Y_VAR; body perms use PUT_Y_VAL for later occurrences and a PUT_VAR-scratch + GET_Y_VAR pair for first-occurrence. refvm gained EnvFrame (saved_CP + N Y-slots), env stack on Vm, and the four opcodes ALLOCATE/DEALLOCATE/GET_Y_VAR/PUT_Y_VAL; choice points now save/restore EP alongside HP/TR. Regenerated ancestor.lam (49 -> 52 cells, query addr 42 -> 45) and ancestor.bin via lam_asm.py. 117 lib + 15 integration tests green; the two new compiled_ancestor refvm scenarios (previously would have infinite-looped) halt correctly.