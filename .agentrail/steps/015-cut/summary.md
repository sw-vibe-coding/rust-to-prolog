Cut (!) compiles to Instr::Cut and refvm dispatches OP_CUT (9) by clearing the choice stack (matches upstream vm_ctrl.plsw's 'BP := CP_BASE' semantics). compile.rs recognizes '!' as a 0-ary inline builtin. examples/max.pl tests cut commitment: max(5,3,M) + fail-query prints '5\n' (cut wins); a control test with the cut removed prints '5\n3\n' (backtracks into the second clause). 119 lib + 31 integration tests green.